{
  "name": "single_tap_ride",
  "version": "1.0.0",
  "description": "A front end to Careem's services that doesn't require a user account",
  "main": "index.js",
  "scripts": {
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Naveed Fida",
  "license": "ISC",
  "devDependencies": {
    "backbone": "^1.3.3",
    "handlebars": "^4.0.6",
    "jquery": "^2.2.4"
  },
  "devDependencies": {
    "browserify": "^14.3.0",
    "exorcist": "^0.4.0"
  }
}
